% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_fireRegimePolys.R
\name{fireRegimePolyTypes}
\alias{fireRegimePolyTypes}
\alias{prepInputsFireRegimePolys}
\title{\code{prepInputsFireRegimePolys}}
\usage{
fireRegimePolyTypes()

prepInputsFireRegimePolys(
  url = NULL,
  destinationPath = tempdir(),
  studyArea = NULL,
  rasterToMatch = NULL,
  type = "ECOREGION",
  subsetType = "intersects"
)
}
\arguments{
\item{url}{character. URL from which to download and prepare fire regime polygons.
Defaults are provided for Canadian ecodistrict, ecoregion, ecoprovince, and ecozone,
as well as national Fire Regime Types and Fire Regime Units from Erni et al. (2020)
\doi{10.1139/cjfr-2019-0191}.}

\item{destinationPath}{character. Path to directory where data will be downloaded.}

\item{studyArea}{\code{sf} object corresponding to the study area of interest.}

\item{rasterToMatch}{\code{SpatRaster} object covering the spatial extent of \code{studyArea},
used as a template for raster layer creation.}

\item{type}{character. The polygon type to use:
Must be one of "ECODISTRICT", "ECOREGION" (default), "ECOPROVINCE", "ECOZONE",
"FRT", or "FRU".
If suggested package \code{bcdata} is installed, can also be one of:
"BECNDT", "BECSUBZONE", or "BECZONE".}

\item{subsetType}{GIS operation to derive \code{fireRegimePolys} based on \code{studyArea}.
One of 'intersects' or 'contains', where 'intersects' (default) is the spatial intersection of
the fire regime zones and \code{studyArea}, and 'contains' includes all fire regime polygons contained
within \code{studyArea} (i.e. crops it to \code{studyArea}).}
}
\description{
Create fire regime polygons for \code{scfmRegime}.
}
\examples{
withr::local_package("terra")
withr::local_package("SpaDES.tools")

## random study area in central Alberta
studyAreaAB <- vect(cbind(-115, 55), crs = "epsg:4326") |>
  project(paste("+proj=lcc +lat_1=49 +lat_2=77 +lat_0=0 +lon_0=-95",
                "+x_0=0 +y_0=0 +units=m +no_defs +ellps=GRS80 +towgs84=0,0,0")) |>
  randomStudyArea(seed = 60, size = 1e10)

studyAreaBC <- vect(cbind(-122.14, 52.14), crs = "epsg:4326") |>
 project(paste("+proj=lcc +lat_1=49 +lat_2=77 +lat_0=0 +lon_0=-95",
                "+x_0=0 +y_0=0 +units=m +no_defs +ellps=GRS80 +towgs84=0,0,0")) |>
  randomStudyArea(seed = 60, size = 1e10)

## may error due to issues with the server hosting the data
try({
  frpEcoregion <- prepInputsFireRegimePolys(studyArea = studyAreaAB, type = "ECOREGION")
  plot(frpEcoregion)
})

## will error if suggested package 'bcdata' not installed
try({
  frpBECNDT <- prepInputsFireRegimePolys(studyArea = studyAreaBC, type = "BECNDT")
  plot(frpBECNDT)
})

frpFRT <- prepInputsFireRegimePolys(studyArea = studyAreaAB, type = "FRT")
plot(frpFRT)

frpFRU <- prepInputsFireRegimePolys(studyArea = studyAreaAB, type = "FRU")
plot(frpFRU)

## cleanup
withr::deferred_run()
}
