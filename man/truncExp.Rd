% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/truncExp.R
\name{pTE}
\alias{pTE}
\alias{dTE}
\alias{qTE}
\alias{rTE}
\alias{qqTE}
\alias{ETE}
\alias{ETEx}
\alias{ExpBar}
\alias{HannonDayiha}
\title{Various tools for a (right) Truncated Exponential Distribution}
\usage{
pTE(x, theta, T)

dTE(x, theta, T)

qTE(p, theta, T)

rTE(n, theta, T)

qqTE(
  x,
  theta = stop("missing theta"),
  T = stop("missing T"),
  plot.it = TRUE,
  xlab = deparse(substitute(x)),
  ylab = deparse(substitute(y)),
  ...
)

ETE(theta, T, a = 0)

ETEx(theta, T, a = 0, shift = 200)

ExpBar(Z)

HannonDayiha(Z, Tspec = 0)
}
\arguments{
\item{x}{TODO}

\item{theta}{TODO}

\item{T}{TODO}

\item{p}{TODO}

\item{n}{TODO}

\item{plot.it}{logical indicating whether to plot the resulting figure}

\item{xlab, ylab}{label text for x- and y-axes}

\item{...}{additional arguments passed to \code{plot()}}

\item{a}{TODO}

\item{shift}{TODO}

\item{Z}{TODO}

\item{Tspec}{TODO}
}
\description{
\eqn{Z ~ TE(\theta, T)}: pdf is \eqn{\frac{\theta \exp(-z \theta)}{1 - \exp(-T\theta)}}
}
\details{
where \code{T} is the truncation point or upper bound and \eqn{\theta} is the shape parameter
in this application, \code{x} are fire sizes \verb{>= shift}, which is a lower bound
and \code{z = log(x / shift)} are the scaled log transformed sizes which seem to
fit a truncated exponential distribution fairly well.

Originally written by Steve in 1999 in support of Cumming CJFR 2001.
Has been in use by BEACONs and was acquired from Pierre Vernier in May 17 2014.
\itemize{
\item \code{pTE()} is the distribution function;
\item \code{dTE()} is the density function;
\item \code{qTE()} is the quantile function;
\item \code{rTE()} is the random generation function.
In fire size applications \code{exp(rTE(n, theta, T)) * shift} will generate \code{n} random fire sizes;
\item \code{qqTE()} produces a quantile-quantile plot of vector \code{x} against a \code{TE(theta, T)};
\item \code{ETE()} is TODO;
\item \code{ETEx()} is TODO;
\item \code{ExpBar()} is TODO;
\item \code{HannonDayiha()} implements the estimator of Hannon and Dayiha (1999),
ported from 1999 C language implementation by SGC June 2004.
}
}
\references{
Patrick M. Hannona & Ram C. Dahiyaa (1999) Estimation of parameters for the truncated
exponential distribution. Communications in Statistics - Theory and Methods 28(11): 2591-2612.
\doi{10.1080/03610929908832440}.
}
